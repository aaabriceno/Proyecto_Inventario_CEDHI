# Variables locales para levantar Inventario CEDHI con Docker Compose.
# Copiar este archivo como `.env` antes de ejecutar Docker:
#   cp .env.example .env

# La app inventario_cedhi es v15; NO cambiar a v16 (rompe compatibilidad).
ERPNEXT_VERSION=v15.99.1

# Solo para desarrollo local. Cambiar si se usa en un entorno compartido.
DB_PASSWORD=admin

# Sitio que crea el servicio create-site la primera vez (y password del admin).
SITE_NAME=inventario.localhost
ADMIN_PASSWORD=admin

DB_HOST=db
DB_PORT=3306
REDIS_CACHE=redis-cache:6379
REDIS_QUEUE=redis-queue:6379

# Debe coincidir con SITE_NAME, y NO debe quedar vacio: fuerza que el
# sitio se sirva siempre, sin importar si el navegador entra por este
# nombre (la misma PC servidor) o por la IP de la LAN (otra PC del CEDHI,
# ej. 192.168.1.X) -- ver server_name "_" en nginx/frappe.conf.template,
# que es lo que permite que nginx acepte la peticion por IP en primer
# lugar (sin eso, aunque este header este bien, nginx la rechaza antes de
# llegar al backend).
FRAPPE_SITE_NAME_HEADER=inventario.localhost

RESTART_POLICY=unless-stopped
